/* --- CSS VARIABLES (Color Palette) --- */
:root {
  --crisp-white: #ffffff;
  --light-silver: #f4f4f4;
  --silver: #b0b0b0;
  --dark-silver: #333333;
  --rose-gold: #b76e79;
  --gold: #d4af37;
  
  --font-heading: 'Montserrat', sans-serif;
  --font-body: 'Playfair Display', serif;
}

/* --- GLOBAL STYLES --- */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: var(--font-body);
  background-color: var(--crisp-white);
  color: var(--dark-silver);
  line-height: 1.6;
}

h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-heading);
  font-weight: 300;
  text-transform: uppercase;
  letter-spacing: 2px;
}

a {
  text-decoration: none;
  color: var(--dark-silver);
  transition: color 0.3s ease;
}

a:hover {
  color: var(--rose-gold);
}

/* --- NAVIGATION --- */
nav {
  position: fixed;
  top: 0;
  width: 100%;
  padding: 2rem 5%;
  display: flex;
  justify-content: space-between;
  align-items: center;
  z-index: 1000;
  background: rgba(255, 255, 255, 0.95);
  border-bottom: 1px solid var(--light-silver);
}

.logo {
  font-family: var(--font-heading);
  font-size: 1.2rem;
  font-weight: 500;
  letter-spacing: 3px;
  color: var(--dark-silver);
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 2rem;
}

.nav-links li a {
  font-family: var(--font-heading);
  font-size: 0.85rem;
  font-weight: 400;
  letter-spacing: 1px;
}

.nav-links li a.active {
  color: var(--gold);
  border-bottom: 1px solid var(--gold);
}

/* --- HOME PAGE (Hero Section) --- */
.hero {
  height: 100vh;
  width: 100%;
  background-image: linear-gradient(rgba(51, 51, 51, 0.3), rgba(51, 51, 51, 0.3)), url('IMG_E0772.jpg'); /* The desert horse image */
  background-size: cover;
  background-position: center;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.hero-content h1 {
  color: var(--crisp-white);
  font-size: 4rem;
  letter-spacing: 5px;
  margin-bottom: 1rem;
  text-shadow: 2px 2px 10px rgba(0,0,0,0.2);
}

.hero-content p {
  color: var(--crisp-white);
  font-family: var(--font-heading);
  font-size: 1.2rem;
  letter-spacing: 3px;
  text-transform: uppercase;
}

.hero-content .highlight {
  color: var(--gold);
}

/* --- ABOUT PAGE --- */
.about-container {
  padding: 150px 10% 100px 10%; /* Offset for fixed header */
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 4rem;
  align-items: center;
  max-width: 1400px;
  margin: 0 auto;
}

.about-image img {
  width: 100%;
  height: auto;
  border: 1px solid var(--light-silver);
  box-shadow: 10px 10px 0px var(--light-silver);
  transition: transform 0.3s ease;
}

.about-image img:hover {
  border-color: var(--rose-gold);
}

.about-text h1 {
  font-size: 2.5rem;
  margin-bottom: 2rem;
  color: var(--dark-silver);
}

.about-text h1::after {
  content: '';
  display: block;
  width: 50px;
  height: 2px;
  background-color: var(--gold);
  margin-top: 15px;
}

.about-text p {
  font-size: 1.1rem;
  margin-bottom: 1.5rem;
  color: var(--dark-silver);
}

/* --- RESPONSIVE DESIGN --- */
@media (max-width: 900px) {
  .about-container {
    grid-template-columns: 1fr;
  }
  
  .nav-links {
    display: none; /* In a full build, add a hamburger menu here */
  }
  
  .hero-content h1 {
    font-size: 2.5rem;
  }
}
